#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

;; tact_PulsePredictionMarket.headers.fc
;;
;; Header files for PulsePredictionMarket
;; NOTE: declarations are sorted for optimal order
;;

;; __tact_context_get
(int, slice, int, slice) __tact_context_get() inline;

;; __tact_context_get_sender
slice __tact_context_get_sender() inline;

;; __tact_slice_eq
int __tact_slice_eq(slice a, slice b) inline;

;; __tact_dict_get_slice_cell
cell __tact_dict_get_slice_cell(cell d, int kl, slice k) inline;

;; __tact_dict_set_slice_cell
(cell, ()) __tact_dict_set_slice_cell(cell d, int kl, slice k, cell v) inline;

;; $Position$_store
builder $Position$_store(builder build_0, (int, int, int) v) inline;

;; $Position$_store_cell
cell $Position$_store_cell((int, int, int) v, builder b) inline;

;; $Position$_load
(slice, ((int, int, int))) $Position$_load(slice sc_0) inline;

;; $Position$_as_optional
tuple $Position$_as_optional((int, int, int) v) inline;

;; $Position$_load_opt
tuple $Position$_load_opt(cell cl) inline;

;; $PulsePredictionMarket$_store
builder $PulsePredictionMarket$_store(builder build_0, (slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell) v) inline;

;; $PulsePredictionMarket$_load
(slice, ((slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell))) $PulsePredictionMarket$_load(slice sc_0) inline;

;; $Context$_get_value
_ $Context$_get_value((int, slice, int, slice) v) inline;

;; $Position$_get_upStake
_ $Position$_get_upStake((int, int, int) v) inline;

;; $Position$_get_downStake
_ $Position$_get_downStake((int, int, int) v) inline;

;; $Position$_get_claimed
_ $Position$_get_claimed((int, int, int) v) inline;

;; $Position$_not_null
((int, int, int)) $Position$_not_null(tuple v) inline;

;; $PulsePredictionMarket$init$_load
(slice, ((slice, int, int, int))) $PulsePredictionMarket$init$_load(slice sc_0) inline;

;; $PulsePredictionMarket$_contract_init
(slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell) $PulsePredictionMarket$_contract_init(slice $admin, int $roundDurationSeconds, int $protocolFeeBps, int $deploymentNonce) impure inline;

;; $PulsePredictionMarket$_contract_load
(slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell) $PulsePredictionMarket$_contract_load() impure inline;

;; $PulsePredictionMarket$_contract_store
() $PulsePredictionMarket$_contract_store((slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell) v) impure inline;

;; $PulsePredictionMarket$_fun_startRound
((slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell), ()) $PulsePredictionMarket$_fun_startRound((slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell) $self, slice $nextMarketId, slice $nextMarketLabel, int $nowTs) impure inline_ref;

;; $PulsePredictionMarket$_fun_requireAdmin
((slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell), ()) $PulsePredictionMarket$_fun_requireAdmin((slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell) $self, slice $candidate) impure inline_ref;

;; $Position$_constructor_upStake_downStake_claimed
((int, int, int)) $Position$_constructor_upStake_downStake_claimed(int $upStake, int $downStake, int $claimed) inline;

;; $SendParameters$_constructor_to_value_mode
((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode(slice $to, int $value, int $mode) inline;


;; tact_PulsePredictionMarket.stdlib.fc
global (int, slice, int, slice) __tact_context;
global slice __tact_context_sender;
global cell __tact_child_contract_codes;
global int __tact_randomized;

(cell, int) __tact_dict_delete(cell dict, int key_len, slice index) asm(index dict key_len) """
    DICTDEL
""";

((cell), ()) __tact_dict_set_ref(cell dict, int key_len, slice index, cell value) asm(value index dict key_len) """
    DICTSETREF
""";

(cell, int) __tact_dict_get_ref(cell dict, int key_len, slice index) asm(index dict key_len) """
    DICTGETREF NULLSWAPIFNOT
""";

(int, slice, int, slice) __tact_context_get() inline {
    return __tact_context;
}

slice __tact_context_get_sender() inline {
    return __tact_context_sender;
}

int __tact_slice_eq(slice a, slice b) inline {
    return (a.slice_hash() ==  b.slice_hash());
}

forall X0, X1, X2 -> tuple __tact_tuple_create_3((X0, X1, X2) v) asm """
    3 TUPLE
""";

forall X0, X1, X2 -> (X0, X1, X2) __tact_tuple_destroy_3(tuple v) asm """
    3 UNTUPLE
""";

cell __tact_dict_get_slice_cell(cell d, int kl, slice k) inline {
    var (r, ok) = __tact_dict_get_ref(d, kl, k);
    if (ok) {
        return r;
    } else {
        return null();
    }
}

(cell, ()) __tact_dict_set_slice_cell(cell d, int kl, slice k, cell v) inline {
    if (null?(v)) {
        var (r, ok) = __tact_dict_delete(d, kl, k);
        return (r, ());
    } else {
        return __tact_dict_set_ref(d, kl, k, v);
    }
}

() $global_send((int, cell, cell, cell, int, slice, int) $params) impure asm """
    NEWC
    b{01} STSLICECONST  // store tag = $0 and ihr_disabled = true
    1 STI               // store `bounce`
    b{000} STSLICECONST // store bounced = false and src = addr_none
    STSLICE             // store `to`
    SWAP
    STGRAMS             // store `value`
    105 PUSHINT         // 1 + 4 + 4 + 64 + 32
    STZEROES            // store currency_collection, ihr_fee, fwd_fee, created_lt and created_at
    // → Stack state
    // s0: Builder
    // s1: `data`
    // s2: `code`
    // s3: `body`
    // s4: `mode`

    // Group 2: Placing the Builder after code and data, then checking those for nullability
    s2 XCHG0
    DUP2
    ISNULL
    SWAP
    ISNULL
    AND
    // → Stack state
    // s0: -1 (true) if `data` and `code` are both null, 0 (false) otherwise
    // s1: `code`
    // s2: `data`
    // s3: Builder
    // s4: `body`
    // s5: `mode`

    // Group 3: Left branch of the IFELSE, executed if s0 is -1 (true)
    <{
        DROP2 // drop `data` and `code`, since either of those is null
        b{0} STSLICECONST
    }> PUSHCONT

    // Group 3: Right branch of the IFELSE, executed if s0 is 0 (false)
    <{
        // _ split_depth:(Maybe (## 5))
        //   special:(Maybe TickTock)
        //   code:(Maybe ^Cell)
        //   data:(Maybe ^Cell)
        //   library:(Maybe ^Cell)
        // = StateInit;
        ROT                // place message Builder on top
        b{10} STSLICECONST // store Maybe = true, Either = false
        // Start composing inlined StateInit
        b{00} STSLICECONST // store split_depth and special first
        STDICT             // store code
        STDICT             // store data
        b{0} STSLICECONST  // store library
    }> PUSHCONT

    // Group 3: IFELSE that does the branching shown above
    IFELSE
    // → Stack state
    // s0: Builder
    // s1: null or StateInit
    // s2: `body`
    // s3: `mode`

    // Group 4: Finalizing the message
    STDICT // store `body` as ref with an extra Maybe bit, since `body` might be null
    ENDC
    // → Stack state
    // s0: Cell
    // s1: `mode`

    // Group 5: Sending the message, with `mode` on top
    SWAP
    SENDRAWMSG // https://github.com/tact-lang/tact/issues/1558
""";

int $global_now() impure asm """
    NOW
""";

;; tact_PulsePredictionMarket.constants.fc
;; String ""
slice __gen_slice_string_96a296d224f285c67bee93c30f8a309157f0daa35dc5b87e410b78630a09cfc7() asm """
    B{b5ee9c724101010100020000004cacb9cd} B>boc <s PUSHSLICE
""";

;; tact_PulsePredictionMarket.storage.fc
;;
;; Type: Context
;; TLB: _ bounceable:bool sender:address value:int257 raw:^slice = Context
;;

_ $Context$_get_value((int, slice, int, slice) v) inline {
    var (v'bounceable, v'sender, v'value, v'raw) = v;
    return v'value;
}

;;
;; Type: SendParameters
;; TLB: _ mode:int257 body:Maybe ^cell code:Maybe ^cell data:Maybe ^cell value:int257 to:address bounce:bool = SendParameters
;;

((int, cell, cell, cell, int, slice, int)) $SendParameters$_constructor_to_value_mode(slice $to, int $value, int $mode) inline {
    return ($mode, null(), null(), null(), $value, $to, true);
}

;;
;; Type: Position
;; TLB: _ upStake:coins downStake:coins claimed:bool = Position
;;

builder $Position$_store(builder build_0, (int, int, int) v) inline {
    var (v'upStake, v'downStake, v'claimed) = v;
    build_0 = build_0.store_varuint16(v'upStake);
    build_0 = build_0.store_varuint16(v'downStake);
    build_0 = build_0.store_int(v'claimed, 1);
    return build_0;
}

cell $Position$_store_cell((int, int, int) v, builder b) inline {
    return $Position$_store(b, v).end_cell();
}

(slice, ((int, int, int))) $Position$_load(slice sc_0) inline {
    var v'upStake = sc_0~load_varuint16();
    var v'downStake = sc_0~load_varuint16();
    var v'claimed = sc_0~load_int(1);
    return (sc_0, (v'upStake, v'downStake, v'claimed));
}

tuple $Position$_as_optional((int, int, int) v) inline {
    var (v'upStake, v'downStake, v'claimed) = v;
    return __tact_tuple_create_3(v'upStake, v'downStake, v'claimed);
}

tuple $Position$_load_opt(cell cl) inline {
    if (null?(cl)) {
        return null();
    }
    var sc = cl.begin_parse();
    return $Position$_as_optional(sc~$Position$_load());
}

_ $Position$_get_upStake((int, int, int) v) inline {
    var (v'upStake, v'downStake, v'claimed) = v;
    return v'upStake;
}

_ $Position$_get_downStake((int, int, int) v) inline {
    var (v'upStake, v'downStake, v'claimed) = v;
    return v'downStake;
}

_ $Position$_get_claimed((int, int, int) v) inline {
    var (v'upStake, v'downStake, v'claimed) = v;
    return v'claimed;
}

((int, int, int)) $Position$_not_null(tuple v) inline {
    throw_if(128, null?(v));
    var (int vvv'upStake, int vvv'downStake, int vvv'claimed) = __tact_tuple_destroy_3(v);
    return (vvv'upStake, vvv'downStake, vvv'claimed);
}

((int, int, int)) $Position$_constructor_upStake_downStake_claimed(int $upStake, int $downStake, int $claimed) inline {
    return ($upStake, $downStake, $claimed);
}

;;
;; Type: PulsePredictionMarket
;; TLB: _ admin:address roundDurationSeconds:int257 protocolFeeBps:int257 deploymentNonce:int257 marketId:^string marketLabel:^string status:int257 openedAt:int257 closesAt:int257 settledAt:int257 totalUp:coins totalDown:coins result:int257 positions:dict<address, ^Position{upStake:coins,downStake:coins,claimed:bool}> = PulsePredictionMarket
;;

builder $PulsePredictionMarket$_store(builder build_0, (slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell) v) inline {
    var (v'admin, v'roundDurationSeconds, v'protocolFeeBps, v'deploymentNonce, v'marketId, v'marketLabel, v'status, v'openedAt, v'closesAt, v'settledAt, v'totalUp, v'totalDown, v'result, v'positions) = v;
    build_0 = build_0.store_slice(v'admin);
    build_0 = build_0.store_int(v'roundDurationSeconds, 257);
    build_0 = build_0.store_int(v'protocolFeeBps, 257);
    var build_1 = begin_cell();
    build_1 = build_1.store_int(v'deploymentNonce, 257);
    build_1 = build_1.store_builder_ref(begin_cell().store_slice(v'marketId));
    build_1 = build_1.store_builder_ref(begin_cell().store_slice(v'marketLabel));
    build_1 = build_1.store_int(v'status, 257);
    build_1 = build_1.store_int(v'openedAt, 257);
    var build_2 = begin_cell();
    build_2 = build_2.store_int(v'closesAt, 257);
    build_2 = build_2.store_int(v'settledAt, 257);
    build_2 = build_2.store_varuint16(v'totalUp);
    build_2 = build_2.store_varuint16(v'totalDown);
    build_2 = build_2.store_int(v'result, 257);
    build_2 = build_2.store_dict(v'positions);
    build_1 = store_builder_ref(build_1, build_2);
    build_0 = store_builder_ref(build_0, build_1);
    return build_0;
}

(slice, ((slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell))) $PulsePredictionMarket$_load(slice sc_0) inline {
    var v'admin = sc_0~load_msg_addr();
    var v'roundDurationSeconds = sc_0~load_int(257);
    var v'protocolFeeBps = sc_0~load_int(257);
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'deploymentNonce = sc_1~load_int(257);
    var v'marketId = sc_1~load_ref().begin_parse();
    var v'marketLabel = sc_1~load_ref().begin_parse();
    var v'status = sc_1~load_int(257);
    var v'openedAt = sc_1~load_int(257);
    slice sc_2 = sc_1~load_ref().begin_parse();
    var v'closesAt = sc_2~load_int(257);
    var v'settledAt = sc_2~load_int(257);
    var v'totalUp = sc_2~load_varuint16();
    var v'totalDown = sc_2~load_varuint16();
    var v'result = sc_2~load_int(257);
    var v'positions = sc_2~load_dict();
    return (sc_0, (v'admin, v'roundDurationSeconds, v'protocolFeeBps, v'deploymentNonce, v'marketId, v'marketLabel, v'status, v'openedAt, v'closesAt, v'settledAt, v'totalUp, v'totalDown, v'result, v'positions));
}

(slice, ((slice, int, int, int))) $PulsePredictionMarket$init$_load(slice sc_0) inline {
    var v'admin = sc_0~load_msg_addr();
    var v'roundDurationSeconds = sc_0~load_int(257);
    var v'protocolFeeBps = sc_0~load_int(257);
    slice sc_1 = sc_0~load_ref().begin_parse();
    var v'deploymentNonce = sc_1~load_int(257);
    return (sc_0, (v'admin, v'roundDurationSeconds, v'protocolFeeBps, v'deploymentNonce));
}

(slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell) $PulsePredictionMarket$_contract_load() impure inline {
    slice $sc = get_data().begin_parse();
    int $loaded = $sc~load_int(1);
    if ($loaded) {
        return $sc~$PulsePredictionMarket$_load();
    }
    else {
        (slice $admin, int $roundDurationSeconds, int $protocolFeeBps, int $deploymentNonce) = $sc~$PulsePredictionMarket$init$_load();
        $sc.end_parse();
        return $PulsePredictionMarket$_contract_init($admin, $roundDurationSeconds, $protocolFeeBps, $deploymentNonce);
    }
}

() $PulsePredictionMarket$_contract_store((slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell) v) impure inline {
    builder b = begin_cell();
    b = b.store_int(true, 1);
    b = $PulsePredictionMarket$_store(b, v);
    set_data(b.end_cell());
}

;;
;; Contract PulsePredictionMarket functions
;;

(slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell) $PulsePredictionMarket$_contract_init(slice $admin, int $roundDurationSeconds, int $protocolFeeBps, int $deploymentNonce) impure inline {
    var (($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions)) = (null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null(), null());
    $self'admin = $admin;
    $self'roundDurationSeconds = $roundDurationSeconds;
    $self'protocolFeeBps = $protocolFeeBps;
    $self'deploymentNonce = $deploymentNonce;
    $self'marketId = __gen_slice_string_96a296d224f285c67bee93c30f8a309157f0daa35dc5b87e410b78630a09cfc7();
    $self'marketLabel = __gen_slice_string_96a296d224f285c67bee93c30f8a309157f0daa35dc5b87e410b78630a09cfc7();
    $self'status = 3;
    $self'openedAt = 0;
    $self'closesAt = 0;
    $self'settledAt = 0;
    $self'totalUp = 0;
    $self'totalDown = 0;
    $self'result = 2;
    $self'positions = null();
    return ($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions);
}

((slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell), ()) $PulsePredictionMarket$_fun_startRound((slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell) $self, slice $nextMarketId, slice $nextMarketLabel, int $nowTs) impure inline_ref {
    var (($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions)) = $self;
    $self'marketId = $nextMarketId;
    $self'marketLabel = $nextMarketLabel;
    $self'status = 0;
    $self'openedAt = $nowTs;
    $self'closesAt = ($nowTs + $self'roundDurationSeconds);
    $self'settledAt = 0;
    $self'totalUp = 0;
    $self'totalDown = 0;
    $self'result = 2;
    $self'positions = null();
    return (($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions), ());
}

((slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell), ()) $PulsePredictionMarket$_fun_requireAdmin((slice, int, int, int, slice, slice, int, int, int, int, int, int, int, cell) $self, slice $candidate) impure inline_ref {
    var (($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions)) = $self;
    throw_unless(47541, ( equal_slices_bits($candidate, $self'admin) ));
    return (($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions), ());
}

;;
;; Get methods of a Contract PulsePredictionMarket
;;

;;
;; Routing of a Contract PulsePredictionMarket
;;

;; message opcode reader utility: only binary receivers
;; Returns 32 bit message opcode, otherwise throws the "Invalid incoming message" exit code
(slice, int) ~load_opcode_internal(slice s) asm( -> 1 0) "32 LDUQ 130 THROWIFNOT";

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    cs~skip_bits(2);
    var msg_bounceable = cs~load_int(1);
    var msg_bounced = cs~load_int(1);
    slice msg_sender_addr = cs~load_msg_addr();
    __tact_context = (msg_bounceable, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var ($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions) = $PulsePredictionMarket$_contract_load();
    
    ;; Handle bounced messages
    if (msg_bounced) { return (); }
    var op = in_msg~load_opcode_internal();
    ;; Receive PlaceBet message
    if (op == 0x50554231) {
        var $msg'marketId = in_msg~load_ref().begin_parse();
        var $msg'marketLabel = in_msg~load_ref().begin_parse();
        var $msg'direction = in_msg~load_uint(8);
        int $nowTs = $global_now();
        if (( (($self'status == 3)) ? (true) : (($self'status == 2)) )) {
            ($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions)~$PulsePredictionMarket$_fun_startRound($msg'marketId, $msg'marketLabel, $nowTs);
        }
        throw_unless(36840, __tact_slice_eq($self'marketId, $msg'marketId));
        throw_unless(15520, ($self'status == 0));
        throw_unless(55144, ($nowTs <= $self'closesAt));
        throw_unless(32630, ($Context$_get_value(__tact_context_get()) > 0));
        if (($msg'direction == 1)) {
            $self'totalUp = ($self'totalUp + $Context$_get_value(__tact_context_get()));
        } else {
            $self'totalDown = ($self'totalDown + $Context$_get_value(__tact_context_get()));
        }
        tuple $existingPosition = $Position$_load_opt(__tact_dict_get_slice_cell($self'positions, 267, __tact_context_get_sender()));
        var ($position'upStake, $position'downStake, $position'claimed) = (null?($existingPosition) ? $Position$_constructor_upStake_downStake_claimed(0, 0, false) : $Position$_not_null($existingPosition));
        if (($msg'direction == 1)) {
            $position'upStake = ($position'upStake + $Context$_get_value(__tact_context_get()));
        } else {
            $position'downStake = ($position'downStake + $Context$_get_value(__tact_context_get()));
        }
        $self'positions~__tact_dict_set_slice_cell(267, __tact_context_get_sender(), $Position$_store_cell(($position'upStake, $position'downStake, $position'claimed), begin_cell()));
        $PulsePredictionMarket$_contract_store(($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions));
        return ();
    }
    
    ;; Receive CloseRound message
    if (op == 0x50554331) {
        var $msg'marketId = in_msg~load_ref().begin_parse();
        ($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions)~$PulsePredictionMarket$_fun_requireAdmin(__tact_context_get_sender());
        throw_unless(35659, __tact_slice_eq($self'marketId, $msg'marketId));
        throw_unless(32385, ($self'status == 0));
        $self'status = 1;
        $PulsePredictionMarket$_contract_store(($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions));
        return ();
    }
    
    ;; Receive SettleRound message
    if (op == 0x50555331) {
        var $msg'marketId = in_msg~load_ref().begin_parse();
        var $msg'result = in_msg~load_uint(8);
        ($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions)~$PulsePredictionMarket$_fun_requireAdmin(__tact_context_get_sender());
        throw_unless(35659, __tact_slice_eq($self'marketId, $msg'marketId));
        throw_unless(59719, ($self'status == 1));
        $self'status = 2;
        $self'result = $msg'result;
        $self'settledAt = $global_now();
        $PulsePredictionMarket$_contract_store(($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions));
        return ();
    }
    
    ;; Receive Claim message
    if (op == 0x50555031) {
        var $msg'marketId = in_msg~load_ref().begin_parse();
        throw_unless(35659, __tact_slice_eq($self'marketId, $msg'marketId));
        throw_unless(61822, ($self'status == 2));
        tuple $position = $Position$_load_opt(__tact_dict_get_slice_cell($self'positions, 267, __tact_context_get_sender()));
        throw_unless(19900, (~ null?($position)));
        throw_unless(45634, (~ $Position$_get_claimed($Position$_not_null($position))));
        int $winningStake = (($self'result == 1) ? $Position$_get_upStake($Position$_not_null($position)) : $Position$_get_downStake($Position$_not_null($position)));
        throw_unless(45775, ($winningStake > 0));
        int $totalPool = ($self'totalUp + $self'totalDown);
        int $winnerPool = (($self'result == 1) ? $self'totalUp : $self'totalDown);
        throw_unless(63018, ($winnerPool > 0));
        int $payout = (($winningStake * $totalPool) / $winnerPool);
        int $fee = (($payout * $self'protocolFeeBps) / 10000);
        int $netPayout = ($payout - $fee);
        var ($updatedPosition'upStake, $updatedPosition'downStake, $updatedPosition'claimed) = $Position$_not_null($position);
        $updatedPosition'claimed = true;
        $self'positions~__tact_dict_set_slice_cell(267, __tact_context_get_sender(), $Position$_store_cell(($updatedPosition'upStake, $updatedPosition'downStake, $updatedPosition'claimed), begin_cell()));
        $global_send($SendParameters$_constructor_to_value_mode(__tact_context_get_sender(), $netPayout, 2));
        $PulsePredictionMarket$_contract_store(($self'admin, $self'roundDurationSeconds, $self'protocolFeeBps, $self'deploymentNonce, $self'marketId, $self'marketLabel, $self'status, $self'openedAt, $self'closesAt, $self'settledAt, $self'totalUp, $self'totalDown, $self'result, $self'positions));
        return ();
    }
    
    ;; Throw if not handled
    throw(130);
}


() __tact_selector_hack_asm() impure asm """
@atend @ 1 {
        execute current@ context@ current!
        {
            // The core idea of this function is to save gas by avoiding unnecessary dict jump, when recv_internal/recv_external is called
            // We want to extract recv_internal/recv_external from the dict and select needed function
            // not by jumping to the needed function by it's index, but by using usual IF statements.

            }END> b> // Close previous builder, now we have a cell of previous code on top of the stack

            <{ // Start of the new code builder
                SETCP0
                // Swap the new code builder with the previous code, now we have previous code on top of the stack
                swap
                // Transform cell to slice and load first ref from the previous code, now we have the dict on top of the stack
                <s ref@

                // Extract the recv_internal from the dict
                dup 0 swap @procdictkeylen idict@ { "internal shortcut error" abort } ifnot
                swap

                // Delete the recv_internal from the dict
                0 swap @procdictkeylen idict- drop
                // Delete the recv_external from the dict (it's okay if it's not there)
                -1 swap @procdictkeylen idict- drop
                // Delete the __tact_selector_hack from the dict
                65535 swap @procdictkeylen idict- drop

                // Bring the code builder from the bottom of the stack
                // because if recv_external extraction is optional, and the number of elements on the stack is not fixed
                depth 1- roll
                // Swap with the dict from which we extracted recv_internal and (maybe) recv_external
                swap

                // Check if the dict is empty
                dup null?
                // Store a copy of this flag in the bottom of the stack
                dup depth 1- -roll
                {
                    // If the dict is empty, just drop it (it will be null if it's empty)
                    drop
                }
                {
                    // If the dict is not empty, prepare continuation to be stored in c3
                    <{
                        // Save this dict as first ref in this continuation, it will be pushed in runtime by DICTPUSHCONST
                        swap @procdictkeylen DICTPUSHCONST
                        // Jump to the needed function by it's index
                        DICTIGETJMPZ
                        // If such key is not found, throw 11 along with the key as an argument
                        11 THROWARG
                    }> PUSHCONT
                    // Store the continuation in c3
                    c3 POP
                } cond

                // Function id is on top of the (runtime) stack
                DUP IFNOTJMP:<{
                    // place recv_internal here
                    DROP swap @addop
                }>

                // Bring back the flag, indicating if the dict is empty or not from the bottom of the stack
                depth 1- roll
                {
                    // If the dict is empty, throw 11
                    11 THROWARG
                }
                {
                    // If the dict is not empty, jump to continuation from c3
                    c3 PUSH JMPX
                } cond
            }> b>
        } : }END>c
        current@ context! current!
    } does @atend !
""";

() __tact_selector_hack() method_id(65535) {
    return __tact_selector_hack_asm();
}